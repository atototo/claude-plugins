# AI Party Plugin 설계 문서

> 다양한 AI 에이전트(Claude, Gemini, Codex)가 파티 모드로 협업하는 플러그인
> 문제에 따라 자율적으로 팀을 구성하고, 분석 → 설계 → 구현 → 리뷰를 수행
> 모든 최종 액션은 사용자 승인 게이트를 거침

---

## 문서 구조

| 문서 | 내용 | 상태 |
|------|------|------|
| **index.md** (이 파일) | 비전, 아키텍처, 레이어, 디렉토리, 요구사항, 로드맵 | — |
| [phase1-2.md](phase1-2.md) | 에이전트, 팀 프리셋, 커맨드, 스펙 | ✅ 완료 |
| [phase3.md](phase3.md) | 오케스트레이션 엔진, 상태머신, 강제 모델, OMC 패턴 | 🔄 진행 중 (v0.8.18) |
| [phase4-6.md](phase4-6.md) | AI OPS 플랫폼, 대시보드, 프로덕션 | 📋 미착수 |
| [architecture-direction.md](architecture-direction.md) | OMC + TaskForce.AI 종합 비교 분석 | ✅ 완료 |
| [migration-v09.md](migration-v09.md) | v0.8 → v0.9 역할 기반 전환 가이드 | 📋 설계 완료 |

---

## 1. 비전과 목표

### 궁극적 비전

AI 에이전트들이 **파티 모드**로 자율 협업하되, 모든 결정권은 사람에게 있는 구조.
이슈가 발생하면 AI들이 파이프라인 단계별로 분석하고, 계획 세우고, 구현하고,
리뷰까지 마친 후 사람에게 승인을 요청한다.

### 핵심 원칙

- **AI 유닛 기반**: PL/Architect 같은 역할이 아닌, Claude/Gemini/Codex 각 AI의 **강점**을 정의
- **팀 조합 프리셋**: 문제 유형에 따라 AI 유닛들의 조합(팀)을 미리 정의하되, 동적 구성도 가능
- **파티 모드**: Agent Teams를 활용하여 **파일 기반 핸드오프** + 시그널 메시징으로 단계별 협업
- **승인 게이트**: 코드 변경, PR 생성, 배포 등 실제 액션은 반드시 사용자 승인 후 실행
- **점진적 확장**: CLI 동작 → 알림 통합 → 대시보드 UI 순서로 발전

### 궁극적 목표 — AI OPS 플랫폼

```
ai-party 플러그인은 "실행 엔진"으로, 별도 AI OPS 플랫폼의 핵심 컴포넌트가 된다.

AI OPS Platform (별도 프로젝트)
  ├── DB에서 프로젝트 정보 로드 (레포, 브랜치 전략, 컨벤션, 환경, 인증)
  ├── 요청 단위로 팀 자동 구성 (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS 필수)
  ├── 파이프라인 실행 → findings 수집 → 승인 게이트
  ├── 웹 대시보드 (모니터링, 승인, 이력, 통계)
  ├── 모바일 접근 (claude remote-control)
  └── 보안 관리 (인증 정보 암호화, 최소 권한 원칙)
```

→ 상세: [phase4-6.md](phase4-6.md)

---

## 2. 아키텍처 개요

### 전체 흐름

```
트리거 (수동 커맨드 / 이슈 감지 / 알림)
    │
    ▼
┌─────────────────────────────────────────┐
│  Host (Claude Code 세션) = Lead          │
│  - 문제 유형 판단                        │
│  - 팀 프리셋 선택 or 동적 구성            │
│  - Agent Teams 세션 생성                 │
│  - 오케스트레이션 전담 (직접 작업 안 함)   │
└──────────┬──────────────────────────────┘
           │ 스폰
    ┌──────┼──────┐
    ▼      ▼      ▼
┌──────┐┌──────┐┌──────┐
│Claude││Gemini││Codex │
│opus  ││sonnet││sonnet│  ← 각자 독립 컨텍스트
│설계  ││+CLI  ││+CLI  │  ← 파일 핸드오프 + 시그널
│리뷰  ││분석  ││구현  │
└──┬───┘└──┬───┘└──┬───┘
   │       │       │
   └───────┼───────┘
           │
    ┌──────┴──────┐
    │  파티 모드    │
    │  - 핸드오프   │
    │  - 태스크 분배 │
    │  - 단계별 리뷰 │
    └──────┬──────┘
           │
           ▼
    Host(Lead)가 결과 종합
           │
           ▼
┌─────────────────────┐
│  승인 게이트          │
│  - 변경 사항 요약     │
│  - diff / PR 링크    │
│  - ✅ 승인 / ❌ 거절  │
└──────────┬──────────┘
           │
           ▼
      사용자 확인 후 실행
```

### 파티 모드 예시

```
[이슈: reservation-deploy NPE 발생]

Host(Lead): "NPE 이슈 감지. bugfix 팀 소집. 3명 스폰"

  gemini-agent(analyst) → claude-agent(architect):
    "로그 분석 결과 ReportServiceImpl.initialize()에서
     Map.of()에 null 전달. ERROR 5건, NPE 2건"

  claude-agent(architect) → codex-agent(builder):
    "Map.of() → HashMap 교체 + null guard 방식으로 가자.
     Collections.unmodifiableMap()으로 불변성 유지 필수"

  codex-agent(builder) → claude-agent(reviewer):
    "코드 수정 + 테스트 추가 완료. 리뷰 부탁"

  claude-agent(reviewer) → Host(Lead):
    "리뷰 완료. PASS"

Host(Lead) → Young: "[승인 요청]
  PR: fix: ReportServiceImpl NPE - Map.of() null 방어
  분석(Gemini) → 설계(Claude) → 구현(Codex) → 리뷰(Claude) 완료
  ✅ 승인 / ❌ 거절"
```

---

## 3. 레이어 구조

```
┌─────────────────────────────────────────────┐
│          Layer 3: 오케스트레이션 엔진          │
│  Host(Lead): 팀 선택 → Agent Teams 세션 관리  │
│  승인 게이트, 결과 종합                       │
│  → phase3.md                                │
├─────────────────────────────────────────────┤
│          Layer 2: 팀 프리셋                   │
│  teams/bugfix.md, teams/devops.md, ...        │
│  AI 에이전트 조합 + 역할 + 워크플로우 정의     │
│  → phase1-2.md §5                           │
├─────────────────────────────────────────────┤
│          Layer 1: AI 에이전트 유닛             │
│  agents/claude-agent.md  (opus)              │
│  agents/gemini-agent.md  (sonnet + CLI)      │
│  agents/codex-agent.md   (sonnet + CLI)      │
│  agents/leader-agent.md  (opus) ← Phase 3 추가│
│  ※ Host = Lead, 에이전트가 아닌 세션 자체      │
│  → phase1-2.md §4                           │
├─────────────────────────────────────────────┤
│          Layer 0: 실행 도구 (기존 재활용)       │
│  scripts/codex_exec.sh                       │
│  scripts/gemini_exec.sh                      │
│  hooks/post-delegate-verify.mjs              │
└─────────────────────────────────────────────┘
```

---

## 4. 디렉토리 구조

```
plugins/ai-party/
├── .claude-plugin/
│   └── plugin.json                    # 플러그인 매니페스트
│
├── agents/                            # Layer 1: AI 에이전트 유닛
│   ├── claude-agent.md                # Claude 네이티브 에이전트
│   ├── gemini-agent.md                # Gemini 활용 에이전트
│   ├── codex-agent.md                 # Codex 활용 에이전트
│   └── leader-agent.md               # 파이프라인 오케스트레이터 (Phase 3)
│
├── teams/                             # Layer 2: 팀 프리셋
│   ├── bugfix.md
│   ├── devops.md
│   ├── dev-backend.md
│   └── dev-frontend.md
│
├── commands/                          # Layer 3: 슬래시 커맨드
│   ├── party.md
│   ├── party-team.md
│   ├── party-status.md
│   └── party-board.md
│
├── skills/                            # 오케스트레이션 스킬
│   └── party-mode/
│       ├── SKILL.md                   # 핵심 라우팅 (~50줄)
│       ├── team-orchestration.md
│       ├── single-agent.md
│       ├── prompt-templates.md
│       └── approval-gate.md
│
├── scripts/                           # 실행 도구
│   ├── codex_exec.sh
│   ├── gemini_exec.sh
│   └── common.sh
│
├── lib/                               # 실행 엔진 모듈 (Phase 3)
│   ├── constants.mjs                  # 상태, 이벤트 타입, 경로 상수
│   ├── atomic-write.mjs               # 원자적 JSON 쓰기
│   ├── session.mjs                    # session.json CRUD + 유효성
│   ├── session-cli.mjs                # 세션 CLI 인터페이스
│   ├── state-machine.mjs              # 파이프라인 상태 전환 엔진
│   ├── state-cli.mjs                  # 상태 전환 CLI
│   ├── events.mjs                     # events.ndjson 기록
│   └── tickets.mjs                    # 티켓 CRUD (Phase 5 대시보드용, 현재 미연동)
│
├── hooks/                             # 이벤트 훅
│   ├── hooks.json                     # 훅 등록 (matcher: Agent, TeamCreate)
│   ├── auto-delegate.mjs              # UserPromptSubmit: 팀 모드 자동 위임
│   ├── post-team-init.mjs             # PostToolUse(TeamCreate): 세션 초기화
│   ├── pre-tool-enforce.mjs           # L3: PreToolUse 기계적 차단
│   ├── pre-tool-remind.mjs            # L1: PreToolUse 컨텍스트 리마인더
│   ├── pre-tool-model-inject.mjs      # PreToolUse: model 자동 주입
│   ├── post-tool-verify.mjs           # L2: PostToolUse 스폰/티켓 검증
│   ├── post-agent-verify.mjs          # PostToolUse: 에이전트 결과 검수
│   └── post-pipeline-state.mjs        # PostToolUse: artifact 감지 → 상태 전환
│
├── CLAUDE.md
├── settings.json
├── package.json
└── .gitignore
```

### 런타임 디렉토리 (.party/)

```
프로젝트루트/
└── .party/                       # .gitignore 제외
    ├── session.json              # 세션 메타데이터
    ├── events.ndjson             # 이벤트 스트림
    ├── tickets/                  # 티켓 (Phase 5 대시보드 연동 시 활성화)
    ├── findings/                 # 단계별 결과물 (핸드오프 핵심)
    │   ├── analysis.md           # ANALYZING → Gemini
    │   ├── design.md             # PLANNING → Claude
    │   ├── implementation.md     # EXECUTING → Codex
    │   └── review.md             # REVIEWING → Claude
    └── approvals/                # 승인 요청 기록
```

---

## 5. 사전 요구사항

### 필수 설치

| 도구 | 설치 | 인증 |
|------|------|------|
| Claude Code CLI | `npm install -g @anthropic-ai/claude-code` | Anthropic API Key |
| Codex CLI | `npm install -g @openai/codex` | `OPENAI_API_KEY` |
| Gemini CLI | `npm install -g @anthropic-ai/gemini-cli` | Google AI 인증 |
| Node.js | 18+ | - |
| Git | 2.x+ | - |

### 필수 설정

```json
// settings.json
{
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  }
}
```

### 검증

```bash
claude plugin validate plugins/ai-party
ls plugins/ai-party/teams/
ls plugins/ai-party/agents/
```

---

## 6. 로드맵

| Phase | 내용 | 버전 | 상태 |
|-------|------|------|------|
| **Phase 1** | 기반 구조 + 에이전트 정의 | v0.1~v0.3 | ✅ 완료 |
| **Phase 2** | 팀 프리셋 + Agent Teams + 커맨드 | v0.5~v0.6 | ✅ 완료 |
| **Phase 3** | 실행 엔진 신뢰성 확보 | v0.7~v0.8.x | 🔄 진행 중 |
| **Phase 3.5** | 역할 기반 전환 | v0.9.0 | 📋 설계 완료 |
| **Phase 4** | AI OPS 플랫폼 MVP (별도 레포) | — | 📋 미착수 |
| **Phase 5** | 웹 대시보드 + 승인 UI | — | 📋 미착수 |
| **Phase 6** | 프로덕션 + 확장 | — | 📋 미착수 |

→ Phase 1-2 상세: [phase1-2.md](phase1-2.md)
→ Phase 3 상세: [phase3.md](phase3.md)
→ Phase 3.5 (v0.9.0): [migration-v09.md](migration-v09.md)
→ Phase 4-6 상세: [phase4-6.md](phase4-6.md)

### 최종 비전

```
AI OPS 자동화 플랫폼
  - 개발자는 프로젝트를 등록하고, 작업을 요청하기만 하면 됨
  - AI들이 팀을 구성하여 분석 → 설계 → 구현 → 리뷰
  - 모든 결과는 대시보드에서 확인
  - 모바일(claude remote-control)로 어디서든 지시 + 승인
  - 사람이 승인하기 전까지 AI는 아무것도 실행하지 않음
```

---

## 참고

### 기술 기반

- Claude Code Subagents: https://code.claude.com/docs/en/sub-agents
- Claude Code Agent Teams: https://code.claude.com/docs/en/agent-teams
- Claude Code Plugins: https://code.claude.com/docs/en/plugins
- Gemini CLI Headless: https://google-gemini.github.io/gemini-cli/docs/cli/headless.html
- Codex CLI: https://github.com/openai/codex

### 선행 프로젝트

- multi-delegate plugin: https://github.com/atototo/claude-plugins
- oh-my-claudecode (OMC): https://github.com/Yeachan-Heo/oh-my-claudecode
- TaskForce.AI: https://taskforce.ai
