{
  "description": "Verify ai-party agent outputs and enforce host review",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/post-agent-verify.mjs",
            "timeout": 90
          }
        ]
      }
    ]
  }
}
